{
  "name": "absolutejs-devices-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/devices build && bun run --cwd packages/devices-capacitor build && bun run --cwd packages/devices-expo build",
    "check": "bun run typecheck && bun test && bun run build && bun run verify:dist",
    "format": "prettier --write .",
    "test": "bun test",
    "typecheck": "bun run --filter '@absolutejs/devices*' typecheck",
    "verify:dist": "bun run scripts/verifyDist.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "latest",
    "typescript": "5.9.3"
  }
}
